@{
    ViewData["Title"] = "Welcome";
}

<h2>Welcome</h2>

<ul>
    @for (int i = 0; i < (int)ViewData["NumTimes"]!; i++) // NOTE: "!"" is the null-forgiving operator
    {
        <li>@ViewData["Message"]</li>
    }
</ul>